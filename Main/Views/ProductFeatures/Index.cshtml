@model IEnumerable<ProductFeatureView>
@using Facade.Pd
@using Pages

@{
    ViewData["Title"] = "Product Feature";
    Layout = "_Index";
}

@Html.ShowTable(Model, (string)ViewBag.SortOrder, (string)ViewBag.SearchString, (int)ViewBag.PageNumber)
